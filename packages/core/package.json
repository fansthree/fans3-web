{
  "name": "@fans3/core",
  "version": "0.0.1",
  "scripts": {
    "build": "tsup src/index.ts --outDir ./lib --format esm,cjs --dts --clean",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "localforage": "^1.10.0"
  },
  "peerDependencies": {
    "lit": "^2",
    "tsup": "^6"
  },
  "license": "MIT",
  "devDependencies": {}
}
